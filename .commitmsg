Terminator lines — day/night boundary for all bodies

Add toggleable terminator lines (◐ button, default off) showing the
exact sunlight boundary on Earth, Moon, and all planets. Lines orient
perpendicular to each body's sun direction and update with time
progression. Always pre-oriented so they're correct when toggled on.
